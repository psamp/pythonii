def make_dictionary(keys, values):

    if len(keys) != len(values):
        raise Exception('Must have equal number of keys and values')

    dictionary = dict()

    
    for index in range(len(keys))

def main():
    names = ['Ahsoka', 'Padme', 'Anakin', 'Obi-Wan']
    scores = [100, 80, 90, 85, 75]

# call main
main()